// Generated by CoffeeScript 1.7.1
(function() {
  var Weather, weatherDefaults, weatherEvent, weatherTurbulence;

  weatherEvent = ["Clear", "Partly Cloudy", "Cloudy", "Rain", "Snow", "Fog", "Storm", "Blizzard"];

  weatherTurbulence = ["None", "Light", "Moderate", "Severe"];

  weatherDefaults = {
    visibility: "50mi",
    windDirection: 0,
    windVelocity: 0,
    windGusts: 0,
    turbulence: weatherTurbulence["None"],
    oat: 75,
    event: weatherEvent["Clear"],
    timeOfDay: 1400
  };

  Weather = (function() {
    function Weather(overrides) {
      this.settings = weatherDefaults;
    }

    return Weather;

  })();

  if (window.flightSim2 == null) {
    window.flightSim2 = {};
  }

  window.flightSim2.initWeather = function(overrides) {
    var weather;
    weather = new Weather();
    return weather;
  };

}).call(this);
